import java.math.BigDecimal;
import java.util.ArrayList;

public class Adder {

    // metoda przyjmująca dwie zmienne typu int (add)

    BigDecimal add(ArrayList<BigDecimal> liczby){
        BigDecimal wynik = null;

        //petla{
        // wynik = wynik.add(liczby.get(i);
        //}

        return wynik;
    }
}
